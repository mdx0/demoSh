(
  git --version || echo Git not installed.
  docker -v || echo Docker not installed.
  java -version || echo Java not installed.
  python --version || python3 --version || echo Python not installed.
  dir
)
